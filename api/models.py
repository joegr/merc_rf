from django.db import models

# Create your models here.
# We're weorking with the default User setup, so we won't need any additional models in the database.
# To allow for cascading delete, we can set delete mode of foreign models to Models.CASCADE
